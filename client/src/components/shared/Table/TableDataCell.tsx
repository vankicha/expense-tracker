const TableDataCell = ({ children }: { children: React.ReactNode }) => {
    return <td className="py-2 px-6 font-medium text-gray-700 text-gray-500">{children}</td>;
};

export default TableDataCell;
